# 表头行选择功能

## 功能概述

拓展了原有的 "Use first row as column headers" 功能，现在用户可以：
1. 手动选择用哪一行作为表头
2. 预览显示导入文件的前20行数据
3. 支持自定义行号输入

## 功能特性

### 1. 灵活的表头选择
- **无表头**: 自动生成列名 (col1, col2, ...)
- **预设选项**: 第1行、第2行、第3行、第4行、第5行、第10行、第20行
- **自定义行号**: 用户可以输入任意行号作为表头

### 2. 增强的预览功能
- **20行预览**: 显示前20行数据，方便用户选择合适的表头行
- **实时更新**: 更改表头行时实时刷新预览
- **数据完整性**: 确保表头和数据行正确对应

### 3. 智能表头处理
- **空值处理**: 空表头自动生成默认列名
- **重复处理**: 重复表头自动重命名
- **越界保护**: 表头行超出范围时的优雅处理

## 支持的文件格式

- ✅ **CSV 文件** - 完全支持自定义表头行
- ✅ **Excel 文件 (.xlsx/.xls)** - 完全支持自定义表头行
- ❌ **JSON/NDJSON 文件** - 不适用（有固定结构）

## 使用方法

1. **选择文件**: 导入 CSV 或 Excel 文件
2. **查看预览**: 系统自动显示前20行数据
3. **选择表头行**: 从下拉菜单中选择合适的表头行
4. **自定义行号**: 如需特定行号，选择 "Custom row..." 并输入行号
5. **确认导入**: 点击 "Import" 完成导入

## 技术实现

### 前端组件 (CsvJsonImport)
- 将复选框替换为行选择器
- 添加自定义行号输入框
- 更新数据绑定和计算属性

### CSV 模块更新
- 支持 `config.header` 参数接收行号
- 智能处理表头和数据行分离
- 保持与现有 API 的兼容性

### Excel 模块更新
- 使用 `header: 1` 获取原始数组数据
- 根据指定行号提取表头
- 处理复杂 Excel 文件结构

### 样式优化
- 响应式布局设计
- 优雅的下拉选择器样式
- 良好的用户交互体验

## 向后兼容性

- 保持现有 API 完全兼容
- 默认行为与原版本一致（第一行作为表头）
- 不影响现有 JSON/NDJSON 导入功能

## 用户体验改进

1. **更直观**: 下拉选择比复选框更直观
2. **更灵活**: 支持任意行号作为表头
3. **更准确**: 20行预览帮助用户做出正确选择
4. **更友好**: 自定义选项满足特殊需求

这个功能大大提升了数据导入的灵活性和准确性，特别适合处理复杂的 CSV 和 Excel 文件。